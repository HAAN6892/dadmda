# 작업: idea log 파일 신규 생성

> 2026-05-03 새벽. 미래 아이디어/백로그 누적용 새 파일 생성.

---

## 작업 범위

`.claude/memory/idea_backlog.md` 파일 신규 생성. 코드 변경 없음. 향후 "지금 안 하지만 미래에 검토" 아이디어 누적용.

---

## decision_log와의 차이

| | decision_log | idea_backlog |
|---|---|---|
| 성격 | 이미 한 결정의 기록 | 나중에 할 일 / 검토할 아이디어의 기록 |
| 시점 | 과거형 ("결정했음") | 미래형 ("검토 예정") |
| 형식 | 결정 N: 제목 + 근거 + 트레이드오프 | 아이디어: 제목 + 출처 + 검토 트리거 |

---

## 파일 본문

`.claude/memory/idea_backlog.md`로 다음 내용 그대로 박기:

```markdown
# 아이디어 백로그

> 미래에 검토할 아이디어, 기능, 인프라 작업의 누적 기록.
> decision_log와 분리: decision_log는 "이미 한 결정", idea_backlog는 "나중에 할 일".
> 각 아이디어는 검토 트리거 또는 적절한 시점이 박혀 있음. 트리거 없는 아이디어는 박지 말 것 (메타 프로젝트 도피 방지).

---

## 활성 아이디어

### 아이디어 1: 임포트 패턴 자동 학습 파이프라인

- **출처**: 2026-05-03 사용자(Hans) 제안
- **내용**: 사용자가 임포트한 데이터에서 기존 Few-shot에 없는 형식 패턴이 등장하면 자동 감지 → 모델 학습 또는 시스템 프롬프트 자동 업데이트
- **왜 지금 안 하는지**:
  - 1차 사용자(배우자) 1명 단계에서는 통계적 유의미성 없음
  - 추가 인프라 필요 (패턴 감지·분류기, 자동 Few-shot 생성, 시스템 프롬프트 자동 업데이트)
  - 사용자 데이터 활용에 대한 동의/프라이버시 이슈 (RLS는 강제하지만 패턴 분석은 별도 동의 영역)
  - 1주차 핵심 = 4축 영점조절 검증, 학습 파이프라인은 그 다음 단계
- **검토 트리거**:
  - 사용자 10명 이상 정착 + 미인식 형식 임포트가 월 5건 이상 발생
  - 또는 동일 메신저 형식이 사용자 사이에서 공통적으로 등장 (예: 밴드, 인스타 DM 등)
- **연관 결정**: 결정 13 (Few-shot 5개) — 1주차에는 운영 수동 학습으로 진입. 새 형식 발견 시 사용자(Hans)가 docs로 받아 시스템 프롬프트 업데이트.

---

## 보류 / 검토 종료 아이디어

(아직 없음. 활성 아이디어가 적절치 않다고 판단되거나 처리 완료된 경우 이 섹션으로 이동.)
```

---

## 실행 절차

### 1단계: 디렉터리 확인

`.claude/memory/` 디렉터리 존재 확인. 이미 다른 memory 파일들 있을 거라 존재할 것.

### 2단계: 파일 생성

`.claude/memory/idea_backlog.md` 파일 생성. 위 본문 그대로 (마크다운 코드블록 안의 내용만, 바깥 ` ``` ` 표시는 제외).

### 3단계: 파일 보고

생성된 파일의 줄 수 + 첫 헤딩 + 마지막 줄 보고.

### 4단계: 커밋

이전 결정 9~15 커밋과 별도로 진행 또는 같이 진행. 별도 진행 권장 (작업 컨텍스트 다름).

커밋 메시지:

```
docs(memory): idea_backlog.md 신규 생성

- 미래 아이디어/백로그 누적용 새 파일
- decision_log("이미 한 결정")와 분리
- 첫 아이디어: 임포트 패턴 자동 학습 파이프라인 (사용자 10명+ 시점 검토)
- 메타 프로젝트 도피 방지를 위해 검토 트리거 명시 필수
```

푸시는 사용자가 직접.

---

## 컨펌 게이트

- [ ] 파일 본문 검토 (특히 "왜 지금 안 하는지" 섹션)
- [ ] decision_log와의 분리 의도 확인
- [ ] 검토 트리거 명시 OK
- [ ] 커밋 메시지 OK
